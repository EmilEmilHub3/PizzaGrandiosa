<Window x:Class="PizzaRabbitMqClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pizza RabbitMQ Client" Height="450" Width="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock FontSize="20" FontWeight="Bold" Text="RabbitMQ ordreoversigt"/>

        <TextBlock Grid.Row="1"
                   Margin="0,8,0,12"
                   Text="Når backend opretter en SalesOrder, dukker den op her via RabbitMQ."/>

        <DataGrid Grid.Row="2"
                  Name="OrdersGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="OrderId" Binding="{Binding SalesOrderId}" Width="*"/>
                <DataGridTextColumn Header="CustomerId" Binding="{Binding CustomerId}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding OrderType}" Width="*"/>
                <DataGridTextColumn Header="Dato" Binding="{Binding Date}" Width="2*"/>
                <DataGridTextColumn Header="Linjer" Binding="{Binding SalesLineCount}" Width="*"/>
                <DataGridCheckBoxColumn Header="Accepted" Binding="{Binding IsAccepted}" Width="*"/>
                <DataGridCheckBoxColumn Header="Posted" Binding="{Binding IsPosted}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
